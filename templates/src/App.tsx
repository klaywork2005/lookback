// Imports the home page.
import HomePage from './assets/frontend components/homepage'

// Defines the application.
const App = () => {
    // Returns the home page.
    return (
        // Holds the page.
        <div>
            {/* Renders the home page. */}
            <HomePage />
        {/* Ends the page container. */}
        </div>
    )
}

// Exports the application.
export default App
