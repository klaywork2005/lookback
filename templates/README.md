# LookBack client

This directory contains the React client.

## Development

```powershell
npm install
npm run dev
```

Open `http://localhost:5173`.

Vite forwards `/api` requests to Django at `http://127.0.0.1:8000`.

## Checks

```powershell
npm run lint
npm run build
```

The production files are written to `dist`.
